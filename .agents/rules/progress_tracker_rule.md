# Rule: Progress Tracker Lifecycle & Invariants

## Execution Workflow
1. **Pre-Task Check:** Always read `progress_tracker.md` (artifact and `./progress_tracker.md`) before beginning any task to determine active phase, current sub-items, and invariants.
2. **Post-Task Update:** Always update both copies of `progress_tracker.md` upon completing work, checking off items `[x]`, updating percentage tables, and logging status.

## Invariants
- Mobile-first responsiveness and $\ge 48\text{px}$ elder-friendly touch targets.
- Local-first Dexie.js offline caching for tickets, checkpoints, and GPS pings.
- Paired Duo identity (`DUO_A`: Utkarsh & Rajnish Ji, `DUO_B`: Cousin & Uncle Ji).
- Free-tier ecosystem: `@google/genai` (`gemini-2.5-flash`), Leaflet OSM tiles, MongoDB Atlas M0.

## Command Execution
- Full standing permission to proactively execute all `node`, `npm`, `npx`, `git`, `bash`, and terminal commands without asking for permission prompts.
